server:
  port: 8081
spring:
  application:
    name: chatbot

  # Set max size upload
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB
      file-size-threshold: 2KB
  datasource:
    url: jdbc:postgresql://localhost:5433/rag_chatbot
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#  servlet:
#    multipart:
#      max-file-size: 10MB
#      max-request-size: 10MB

  ai:
    model:
      chat: google-genai
    google:
      genai:
        api-key: AIzaSyCnzKLw8bAiWExMQuaqfDcwYktuhQ7ny0g
        chat:
          options:
            model: gemini-2.5-flash
        embedding:
          api-key: AIzaSyCnzKLw8bAiWExMQuaqfDcwYktuhQ7ny0g
          text:
            options:
              models: text-embedding-004

  # Fix error upload file - comment this and save data in kb_embeddings table
    # vectorstore:
    #   pgvector:
    #     index-type: HNSW
    #     distance-type: COSINE_DISTANCE
    #     dimensions: 768



jwt:
  signerKey : "4vCM6CA5NXhXhG+LjHY+PfQRZYGjm13cHoNxVPuDyEYz2XB5SO/8Ko2vCxBkqHeT"
  access-token-time : 2000
  refresh-token-time : 10000

# Cohere API Configuration
cohere:
  api:
    key: xsvbz9deCbpv0AHiHIbzTom0YJNgMh1lYZSS6SI5  # Set COHERE_API_KEY environment variable
  rerank:
    model: rerank-multilingual-v3.0
    top-n: 5

# Document Metadata Configuration
document:
  metadata:
    # Metadata fields available for documents
    fields:
      - id          # Document unique identifier
      - title       # Document title
      - content     # Document content/text
    # Default metadata structure
    structure:
      id: "string"      # Document ID (UUID or string)
      title: "string"   # Document title
      content: "text"   # Document content/nội dung

# Retrieval Pipeline Configuration
retrieval:
  hybrid-top-k: 50
  rerank-top-k: 30
  final-top-k: 5
  enable-crag: true
  filter:
    enabled: true
    mode: INCLUDE  # INCLUDE or EXCLUDE
    # Filter rules based on metadata fields (id, title, content)
    # Example: Filter by document id or title
    rules: {}  # Example: {id: ["doc-id-1", "doc-id-2"], title: ["Document Title 1"]}

# CRAG Configuration
crag:
  max-depth: 2  # Maximum recursion depth for active retrieval